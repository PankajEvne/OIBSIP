{
  "name": "oibsip-pizza-delivery",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "server/index.js",
  "scripts": {
    "postinstall": "npm --prefix server install && npm --prefix client install && npm --prefix client run build",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server start",
    "dev": "npm --prefix server start"
  }
}
